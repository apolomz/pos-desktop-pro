spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/posdb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  sql:
    init:
      mode: never